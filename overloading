package com.company;

public class overloading {
    static void foo(){                                                                                 //method1 
        System.out.println("good morning bro!");     
    }
    static void foo(int a){                                                                            //method2 
        System.out.println("good morning "+a +"bro");
        foo();
        foo(400);
    }
}

TWO OR MORE METHODS HAVING SAME SAME  BUT DIFFERENT PARAMETERS ARE CALEED METHOD OVERLOADING 
in the above there are two methods with similar name but with different parameters . 
